<h2>Ignoring generated files in svn</h2>

<p>
Unless you want to commit Selleck generated files back to repository you should ignore them.
</p>

```terminal
#appending build_docs to svn:ignore property
svn propset svn:ignore "$(svn propget svn:ignore .)
build_docs" .

#committing last changes
svn commit --depth empty -m 'svn ignoring build_docs' .
```

<p>
Of course you can adapt the lines above to other CMS solutions.
</p>
